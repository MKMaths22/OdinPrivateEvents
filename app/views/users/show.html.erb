<% if  @user.events.size.positive? %>
  Here are the Events created by 
  <%= @user[:name] %>:
  <br>
  <br>
  <%= render partial: '/display_events', locals: {events_to_display: @user.events} %>
<% else %>
  <%= @user[:name] %>
  currently has no created Events.
<% end %>