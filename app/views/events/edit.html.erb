Editing Event
<%= @event.id %>
<br>
<br>
<% if @event.creator_id == current_user.id %>
  <%= render partial: 'event_form' %>
<% else %>
  Only the Event's creator is authorised to edit its details.
<% end %>